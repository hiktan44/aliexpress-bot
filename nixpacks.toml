[phases.setup]
nixPkgs = ['python3', 'pip']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build completed"']

[start]
cmd = 'python3 aliexpress_bot_improved_captcha.py'
